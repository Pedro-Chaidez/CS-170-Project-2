# CS 170 Project 2
Feature Selection with Nearest Neighbor

# To build it you must have cmake installed and do the following:
* cmake -G "MinGW Makefiles" .
* cmake --build .
* start ./bin/SearchProgram.exe
